# TODO: raise api endpoints to static

# static_context = TextVerified(xyz)
# __dict__ <- static_context.__dict__ (but only the methods and attributes that are not private, and in such a way that updates to the __dict__ are reflected in the static_context.__dict__)